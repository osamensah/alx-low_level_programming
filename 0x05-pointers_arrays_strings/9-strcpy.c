#include "main.h"

/**
 * *_strcpy - copies a string
 * @dest: points to a buffer
 * @src: points to a string
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
